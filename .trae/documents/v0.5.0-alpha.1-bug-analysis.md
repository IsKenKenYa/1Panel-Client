# v0.5.0-alpha.1+1 测试问题分析报告

**测试环境：**
- 设备：小米13，MIUI 14
- 版本：v0.5.0-alpha.1+1
- 接入方式：HTTPS + 域名 + 证书（easytier内网隧道）

---

## 一、BUG问题分析（13个）

### ✅ 已解决（2个）

#### 1. 服务器删除按钮缺失
**状态：** ✅ 已实现  
**位置：** `lib/features/server/server_detail_page.dart:29`  
**说明：** AppBar中已有删除按钮，带二次确认对话框

#### 2. 服务器硬盘信息显示"--"
**状态：** ✅ Dashboard API已完整实现  
**位置：** `lib/features/dashboard/dashboard_page.dart`  
**说明：** 可能是数据加载时机问题，建议检查网络请求是否成功

---

### 🔴 P0 - 阻塞性问题（5个）

#### 3. TLS证书验证 - HTTPS IP地址无法登录
**状态：** ⚠️ 部分实现  
**文件：** `lib/core/network/dio_client.dart:48-62`  
**分析：**
- 代码中已有 `allowInsecureTls` 参数和证书忽略逻辑
- 需要检查：登录页面是否提供了"忽略证书验证"的UI选项
- **建议：** 检查 `lib/features/auth/` 或 `lib/features/server/server_form_page.dart` 是否有对应的UI开关

#### 4. 数据库页面报错
**错误信息：** 字段验证失败（Database/OrderBy/Order required tags）  
**文件：** `lib/features/databases/databases_page.dart`  
**分析：**
- 可能是API请求参数缺少必填字段
- 需要检查：`DatabasesProvider` 中的API调用参数
- **需要：** 完整的错误堆栈信息

#### 5. 防火墙页面404错误
**文件：** `lib/features/firewall/firewall_page.dart` ✅ 文件存在  
**分析：**
- 页面代码已实现，404可能是：
  1. 路由配置问题（检查 `lib/config/app_router.dart`）
  2. API端点不存在或路径错误
  3. 服务器端防火墙模块未启用
- **需要：** 具体的404错误信息（是页面404还是API 404）

#### 6. 终端页面404错误
**文件：** `lib/features/terminal/terminal_page.dart` ✅ 文件存在  
**分析：**
- 页面代码已实现，WebSocket连接逻辑完整
- 404可能是：
  1. 路由配置问题
  2. WebSocket端点路径错误
  3. 服务器端终端模块未启用
- **需要：** 具体的404错误信息

#### 7. 文件上传无弹窗选择文件
**文件：** `lib/features/files/files_page.dart`  
**分析：**
- 需要检查 `upload_dialog.dart` 的实现
- 可能缺少 `file_picker` 或 `image_picker` 依赖
- **建议：** 检查 `pubspec.yaml` 中是否有文件选择器依赖

---

### 🟡 P1 - 重要问题（6个）

#### 8. 应用商店安装页面提示异常（全部应用）
**文件：** `lib/features/apps/apps_page.dart`  
**分析：**
- 需要检查应用详情页和安装流程
- 可能是：
  1. 应用详情API返回数据格式问题
  2. 安装参数验证失败
- **需要：** 具体的错误提示内容和堆栈

#### 9. 安全页面报错
**错误信息：** type cast error (String vs Map<String, dynamic>)  
**文件：** `lib/features/security/security_verification_page.dart`  
**分析：**
- 典型的数据类型转换错误
- 可能是API返回的数据结构与预期不符
- **需要：** 完整的错误堆栈和API响应数据

#### 10. 容器升级报错
**错误信息：** Names field validation failure  
**文件：** `lib/features/containers/containers_page.dart`  
**分析：**
- API参数验证失败，可能是：
  1. `Names` 字段为空或格式错误
  2. API要求的参数结构变更
- **需要：** 完整的错误信息和API请求参数

#### 11. 容器网络信息无显示
**文件：** `lib/features/orchestration/network_page.dart`  
**分析：**
- 需要检查 `NetworkProvider` 的数据加载逻辑
- 可能是：
  1. API调用失败（静默失败）
  2. 数据解析错误
  3. UI渲染条件判断问题
- **需要：** 检查是否有网络请求错误日志

#### 12. 容器编排信息无显示
**文件：** `lib/features/orchestration/compose_page.dart`  
**分析：**
- 需要检查 `ComposeProvider` 的数据加载逻辑
- 可能原因同上
- **需要：** 检查是否有网络请求错误日志

#### 13. 监控设置保存失败
**文件：** `lib/features/monitoring/`  
**分析：**
- 需要检查监控设置的保存API调用
- 可能是：
  1. API参数错误
  2. 权限不足
  3. 服务器端验证失败
- **需要：** 保存失败的具体错误信息

---

## 二、UI优化问题（6个）

这些属于UI/UX改进，不是BUG，建议单独创建feature spec处理：

1. **容器统计模块固化** - 占用过多屏幕空间
2. **容器管理按钮布局** - 四个按钮应在同一行
3. **文件操作图标** - 需要文字标签（复制、粘贴等）
4. **API密钥显示格式** - 显示异常
5. **安全入口显示** - 显示异常
6. **编排位置** - 建议固定在第二位

---

## 三、功能需求（5个）

这些是新功能，不是BUG修复，建议单独创建feature spec：

1. **文件管理路径记忆** - 记住上次退出路径
2. **编排路径选择** - 增加路径选择器
3. **容器终端功能** - 容器操作中增加终端
4. **容器操作确认** - 停止/启动/重启增加确认和状态反馈
5. **Debug模式** - 快速提取错误日志

---

## 四、下一步建议

### 方案A：逐个修复（推荐）
选择1-2个最紧急的P0问题，提供详细信息后创建bugfix spec：
- 完整的错误堆栈
- 具体的复现步骤
- 相关的API请求/响应数据

### 方案B：批量创建GitHub Issues
将所有问题创建为GitHub Issues，附上：
- 截图
- 错误日志
- 复现步骤
- 优先级标签

### 方案C：启用Debug日志
1. 在应用中启用详细日志
2. 复现所有问题
3. 导出完整日志
4. 基于日志信息创建详细的bugfix spec

---

## 五、快速验证建议

### 立即可验证的问题：

1. **TLS证书选项** - 检查登录/添加服务器页面是否有"忽略证书验证"开关
2. **路由配置** - 检查 `lib/config/app_router.dart` 中防火墙和终端的路由是否正确注册
3. **文件选择器** - 检查 `pubspec.yaml` 是否有 `file_picker` 依赖

### 需要日志的问题：

4-13号问题都需要详细的错误日志才能准确定位根本原因。

---

**总结：** 13个BUG中，2个已解决，11个需要更多信息才能创建准确的bugfix spec。建议优先处理P0问题，并提供详细的错误日志。
