- [x] 页面层级没有对可能为 `null` 的列表、对象强行使用 `!` 操作符（空指针安全）。
- [x] 异步操作（如点击按钮后等待 API 返回）前会显示 Loading 态，操作后会显示 SnackBar 或 Dialog 提示。
- [x] 在异步操作的 `then` 或 `catchError` 中，如果页面已被销毁（`!mounted`），不再调用 `setState`。
- [x] Dashboard 大盘的 CPU、内存、网络图表及进程列表可以正常渲染，无数据脱节或白屏。
- [x] App 商店和已安装列表能够正确显示应用名称、版本、运行状态及对应操作。
- [x] Container 列表和详情页能展现真实日志、环境变量，操作按钮响应正常。
- [x] File 列表能够正确渲染文件夹、文件的大小和时间，并支持交互（点击、长按等）。
- [x] Website、Database、Host 等周边模块的配置获取页面（如 CA 证书、跨域配置）能够展示为正常 UI 组件。
- [x] 即使 API 返回异常，UI 也能够展示友好的 Empty State（如“暂无数据”）或 Error State（如“网络连接失败”）。
- [x] 工作区根目录干净，无遗留的临时日志或检查脚本残留产物（如 `issues.txt`）。