# Tasks

- [ ] **Task 1: Container Advanced Actions**
    - [ ] 1.1 Implement `RenameContainerDialog`.
    - [ ] 1.2 Implement `CommitContainerDialog`.
    - [ ] 1.3 Update `ContainerCard` to include `PopupMenuButton` with Rename, Upgrade, Commit, Edit.
    - [ ] 1.4 Update `ContainersProvider` to support these actions (if not already present or need wrapping).
    - [ ] 1.5 Add "Prune" action to `ContainersPage` AppBar.

- [ ] **Task 2: Image Advanced Actions**
    - [ ] 2.1 Implement `TagImageDialog`.
    - [ ] 2.2 Update `ImageCard` to include `PopupMenuButton` with Tag, Push, Save.
    - [ ] 2.3 Update `DockerImageProvider` to support these actions.

- [ ] **Task 3: App Management Enhancements**
    - [ ] 3.1 Add "Sync" action to `AppStorePage` AppBar.
    - [ ] 3.2 Implement "Ignore Update" in `InstalledAppDetailPage` (add to `_handleAction` or Update dialog).
