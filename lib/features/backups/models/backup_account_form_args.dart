import 'package:onepanel_client/data/models/backup_account_models.dart';

class BackupAccountFormArgs {
  const BackupAccountFormArgs({
    this.initialValue,
  });

  final BackupAccountInfo? initialValue;
}
